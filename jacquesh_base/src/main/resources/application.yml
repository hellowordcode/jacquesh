server:
  port: 9001
spring:
  application:
    name: tensquare-base #指定服务名
  datasource:
    url:
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: root
  jpa:
    database: mysql
    show‐sql: true
    generate‐ddl: true